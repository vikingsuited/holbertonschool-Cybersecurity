# Nmap Live Hosts Discovery

## 0-arp_scan.sh

### Description
This script uses `nmap` with the `-sn` and `-PR` flags to identify active devices on a subnetwork.

### Usage
```bash
sudo ./0-arp_scan.sh <subnetwork> 
